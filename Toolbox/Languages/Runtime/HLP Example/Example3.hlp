Program Example
	Variables a := 2, b := a * 2, c, d.
	
	Task T1
		If (a > b) Then
			c := a + b.
		Else
			d := b - a.
		End.
	End.
End.

// w(a); r(a); w(b); w(c); w(d)

//r(a); r(b)

//(r(a); r(b); w(c)) || (r(b); r(a); w(d))
